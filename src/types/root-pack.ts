/** Represents the roots of a quadratic equation. */
export default interface RootPack {
  /** The first root of the quadratic equation. */
  r1: number

  /** The second root of the quadratic equation. */
  r2: number
}
