/** Represents a set of coefficients for a quadratic equation. */
export default interface CoefficientPack {
  /** The coefficient of the quadratic term. */
  a: number

  /** The coefficient of the linear term. */
  b: number

  /** The constant term. */
  c: number
}
